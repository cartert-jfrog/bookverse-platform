services:
  - name: inventory
    apptrust_application: ${PROJECT_KEY:-bookverse}-inventory
    docker:
      registry: "${DOCKER_REGISTRY}"
      repository: ${PROJECT_KEY:-bookverse}-inventory-internal-docker-release-local/inventory
    simulated_version: 1.8.3
    simulated_digest: sha256:abc123

  - name: recommendations
    apptrust_application: ${PROJECT_KEY:-bookverse}-recommendations
    docker:
      registry: "${DOCKER_REGISTRY}"
      repository: ${PROJECT_KEY:-bookverse}-recommendations-internal-docker-release-local/recommendations-api
    simulated_version: 0.9.0
    simulated_digest: sha256:def456

  - name: checkout
    apptrust_application: ${PROJECT_KEY:-bookverse}-checkout
    docker:
      registry: "${DOCKER_REGISTRY}"
      repository: ${PROJECT_KEY:-bookverse}-checkout-internal-docker-release-local/checkout-api
    simulated_version: 0.7.2
    simulated_digest: sha256:ghi789

  - name: web
    apptrust_application: ${PROJECT_KEY:-bookverse}-web
    docker:
      registry: "${DOCKER_REGISTRY}"
      repository: ${PROJECT_KEY:-bookverse}-web-internal-docker-release-local/web
    simulated_version: 0.4.1
    simulated_digest: sha256:jkl012


